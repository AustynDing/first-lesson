
int main()
{
	int i = 0;
	int j = 0;
	int m = 0;
	for (i = 1; i <= 9; i++)
	{
		for (j = 1; j <= i; j++)
		{
			m = i * j;
			printf("%d*%d=%-3d", j, i, m);
		}
		printf("\n");
	}
	return 0;
}
需要注意的几点：
1.想要一行一行空格可以采用printf("\n");的方式
2.printf("%d*%d=%-3d", j, i, m); 由此可得 %d可以当作是数的映射即三个%d分别代表j i m
3.%d：即为普通的输出。
  %2d：按宽度为2输出，右对齐方式输出。若不够两位，左边补空格。
  %02d：同样宽度为2，右对齐方式。位数不够，左边补0。
  %-2d:按宽度为2输出，左对齐。若不够两位，右边补空格
  %-3d则是按宽度为3输出
